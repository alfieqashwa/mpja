import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { BookOpen, ChevronRight, GraduationCap, Star } from "lucide-react";

export function FeatureCourses() {
  return (
    <section id="course" className="py-16 md:py-24 bg-white">
      <div className="text-rose-500 text-center font-bold mb-2 uppercase tracking-wide text-sm">
        Featured Courses
      </div>
    </section>
  );
}
